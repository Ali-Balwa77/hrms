<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /dp/hrms/

  # Existing files/folders direct serve
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # React SPA route fallback
  RewriteRule . /dp/hrms/index.html [L]
</IfModule>
